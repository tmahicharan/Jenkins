pipeline {
    agent {
        label 'AGENT-1'
    }
    stages {
        stage ('Build') {
            steps{
                echo "Build"
            }
        }
        stage ('Test') {
            steps {
                echo "Testing"
            }
        }
        stage ('Deploy') {
            steps {
                echo "Deploy"
            }
        }

    }
    post{
        always{
            echo "I will run even pipeline fail"
            cleanWs()
        }
        success{
            echo "I will run if sucess"
        }
        failure{
            echo "I will run if failure"
        }
    }
}